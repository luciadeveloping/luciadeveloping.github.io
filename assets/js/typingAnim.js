
document.addEventListener('DOMContentLoaded', function() {
    var options = {
      strings: ["Lucía", "luciadeveloping", "Lucía Sánchez"],
      typeSpeed: 50,
      backSpeed: 50,
      backDelay: 2000,
      startDelay: 0,
      loop: true,
      showCursor: true,
    };

    var typingNameAnim = new Typed('.typingNameAnim', options);
  });